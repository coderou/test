1.复习:
  1.点击三级导航搜索功能:改变地址,监视$route的改变,发送请求
  2.监视$route变化->调用search()->search方法中通过data的options发送请求
  3.点击trademark商标,将数据更新options,通过search发送请求
  4.search函数通过props传递给子组件[还可以用自定义事件]
  5.props数据结构:id:value:key
  6.当点击prop,将属性push进去props[]
    判断:新添加的props是否已经存在了
    if(newOptions.props){
      if(props.indexOf(newProp)!==-1)return
      判断:要添加prop,且prop之前没有添加过
      props.push(newProp)
    }
  7.升序降序
    order字段数据结构:1:desc  2:asc等,默认值是1:desc
2.初步实现分页器功能:
  #components/Pagination/index.vue
  {
    <template>
      <button @click="changeMyCurrentPage(-1)">👈</button>
      <button @click="clickChange(1)" :class="{ active: myCurrentPage === 1 }">
        1
      </button>
      <button v-show="startEnd.start > 2">...</button> 
      <button 
        :class="{ active: num + startEnd.start - 1 === myCurrentPage }"
        v-for="num in middleBtnCount" 
        :key="num"
        @click="clickChange(num + startEnd.start - 1)"
      >
        {{num+startEnd.start-1}}
      </button> 
      <button v-show="startEnd.end < totalPage-1">...</button> 
      <button
        v-if="totalPage !== 1"
        :class="{ active: myCurrentPage === totalPage }"
        @click="clickChange(totalPage)"
      >
        {{ totalPage }}
      </button>
      <button @click="changeMyCurrentPage(1)">👉</button>
      <select v-model="myPageSize">
        <option :value="size" v-for="size in pageSizes" :key="size"
          >每页{{ size }}条</option
        >
      </select>
      <span class="pagination-total">总数:{{ total }}</span>
    </template>
    <script>
      export default{
        data(){
          return{
            myCurrentPage: this.currentPage,
            myPageSize: this.pageSize,
          }
        },
        props:{
          'page-sizes': {
            type: Array,
            default: () => [5, 10, 15, 20],
          },
        },
        computed:{
          middleBtnCount(){
            const num=this.startEnd.end-this.startEnd.start+1
            return num<0?:0:num
          },
          totalPage() {
            return Math.ceil(this.total / this.myPageSize);
          },
          startEnd(){
            const {} =this;
            // 如果总按钮数量(计算结果),小于按钮数量(7)
            if(totalPage<=pageCount){
              const start=2;
              const end=totalPage-1;
              return{start,end};
            }
            // 正常情况
            const middleBtnCount = pageCount - 2;
            const halfMiddleBtnCount = Math.floor(middleBtnCount / 2);

            let start = myCurrentPage - halfMiddleBtnCount;
            // 特殊情况1(start计算结果小于了最小值)
            if (start < 2) { start = 2; }
            let end = start + middleBtnCount - 1;
            // 特殊情况2(end计算结果大于了最大值)
            if (end >= totalPage) {
              end = totalPage - 1;
              start = end - middleBtnCount + 1;
            }
            return {start,end};
          }
        },
        methods:{
          changeMyCurrentPage(num) {
            if (num === 1) {
              if (this.myCurrentPage >= this.totalPage) return;
              this.myCurrentPage += 1;
            } else {
              if (this.myCurrentPage <= 1) return;
              this.myCurrentPage -= 1;
            }
          },
          // 只有一行代码,可以直接赋值
          clickChange(num) {
            this.myCurrentPage = num;
          },
        },
        watch: {
          myPageSize() {
            const totalPage = Math.ceil(this.total / this.myPageSize);
            if (this.myCurrentPage > totalPage) { this.myCurrentPage = totalPage; }
          },
        },

      };
    </script>
  }
3.props默认值是数组会报错,所以要写成函数形式,函数返回数组
4.使用ctrl+.代替格式化
5.tip:结构出来有利于性能优化,因为多次读取操作也会消耗性能
6.watch用的都可以用computed来使用???
7.computed不可以写异步操作??
8._lodash使用防抖节流
9.lodash函数:哪里要用,在那个函数包一层即可
10.改变当前页,更新页面
  tip:yarn add lodash
  #view/Search/Search.vue
  {
    <template>
      <Patination
        @size-change="handleSizeChange"
        @current-change="handleCurrentChange"
        :current-page="options.pageNo"
        :page-size="options.pageSize"
        :page-sizes="[5, 10, 15, 20]"
        :total="total"
        :page-count="7"
      />
    </template>
    <script>
      // import _ from 'lodash';// 完整引入,打包体积大
      import debounce from 'lodash/debounce';// 单独引入
      export default{
        methods:{
          //handleCurrentChange: _.debounce(function (currentPage) {
          handleCurrentChange: debounce(function (currentPage) {
            this.options.pageNo = currentPage;
            this.search();
          },
          300,
          {
            // 让延迟开始前调用一次
            leading: true,
          }),
        },
      }
    </script>
  }
  #components/Pagination/index.vue
  {
    <script>
      export default{
        watch:{
          myPageSize(pageSize) {
            // const totalPage = Math.ceil(this.total / this.myPageSize);
            // if (this.myCurrentPage > totalPage) { this.myCurrentPage = totalPage; }
            // tip:结构出来有利于性能优化,因为多次读取操作也会消耗性能
            const { myCurrentPage, totalPage } = this;
            if (myCurrentPage > totalPage) {
              this.myCurrentPage = totalPage;
            }
            this.$emit('size-change', pageSize);
          },
          myCurrentPage(myCurrentPage) {
            this.$emit('current-change', myCurrentPage);
          },
        }
      }
    </script>
  }
11.